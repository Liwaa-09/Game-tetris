{
  "name": "tetris",
  "version": "1.0.0",
  "description": "The classic Tetris game with pure JS",
  "private": true,
  "dependencies": {
    "some-framework": "^1.0.0" // Tambahkan framework yang sesuai jika ada
  },
  "devDependencies": {
    "css-loader": "^4.2.1",
    "html-webpack-plugin": "^4.3.0",
    "style-loader": "^1.2.1",
    "terser-webpack-plugin": "^4.1.0",
    "webpack": "^4.44.1",
    "webpack-cli": "^3.3.12"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack --config webpack.config.js", // Sesuaikan dengan konfigurasi build Anda
    "deploy": "surge --project ./dist/ --domain tetromino.surge.sh"
  }
}
